{
  "name": "my-chat-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "my-chat mobile client — Capacitor + Vanilla TS",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "cap:sync": "npx cap sync",
    "cap:ios": "npm run build && npx cap sync ios && npx cap open ios",
    "cap:run:ios": "npm run build && npx cap sync ios && npx cap run ios"
  },
  "dependencies": {
    "@aparajita/capacitor-biometric-auth": "^10.0.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.4.2",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^3.2.7"
  }
}
